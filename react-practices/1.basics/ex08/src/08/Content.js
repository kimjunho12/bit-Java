import React, { Fragment } from "react";
import Clock from "./Clock";

const content = () => {
  return (
    <Fragment>
      <p>{`Comment`}</p>
      <Clock />
    </Fragment>
  );
};

export default content;
